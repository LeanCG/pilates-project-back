<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Agregar Ejercicio</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container mt-5">
        <h3>Agregar Ejercicio</h3>
        <!-- Formulario para agregar rutina y ejercicios -->
        <form id="addExerciseForm">
            <!-- Input para agregar la rutina una sola vez -->
            <div class="row mb-3">
                <div class="col-md-12">
                    <label for="rutina" class="form-label">Rutina</label>
                    <input type="text" id="rutina" name="rutina" class="form-control" required>
                </div>
            </div>

             <!-- Campo oculto para el ID de la rutina -->
             <input type="hidden" id="rutina_id" name="rutina_id" value="">

            <!-- Campos dinámicos para agregar ejercicios -->
            <div id="exerciseContainer">
                <div class="row mb-3">
                    <div class="col-md-6">
                        <label for="ejercicio" class="form-label">Ejercicio</label>
                        <select id="ejercicio" name="ejercicio[]" class="form-select" required>
                            <option value="">Seleccione un ejercicio</option>
                            <% // Aquí se debe iterar sobre los ejercicios de la base de datos %>
                        </select>
                    </div>
                    <div class="col-md-3">
                        <label for="series" class="form-label">Series</label>
                        <input type="number" id="series" name="series[]" class="form-control" required>
                    </div>
                    <div class="col-md-3">
                        <label for="repeticiones" class="form-label">Repeticiones</label>
                        <input type="number" id="repeticiones" name="repeticiones[]" class="form-control" required>
                    </div>
                </div>
            </div>
            <div class="d-flex justify-content-end">
                <button type="button" id="addExerciseButton" class="btn btn-primary me-2">Agregar Ejercicio</button>
                <button type="submit" class="btn btn-success">Guardar</button>
            </div>
        </form>

        <!-- Tabla para mostrar los ejercicios agregados -->
        <table class="table table-bordered table-striped mt-3" id="exerciseTable">
            <thead>
                <tr>
                    <th>Ejercicio</th>
                    <th>Series</th>
                    <th>Repeticiones</th>
                </tr>
            </thead>
            <tbody>
                <!-- Aquí se irán agregando las filas de ejercicios con JavaScript -->
            </tbody>
        </table>
    </div>

    <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
    <script src="/js/exercise.js"></script>
</body>
</html>